node_modules
.next
out
build
coverage
package-lock.json
# Generated by drizzle-kit; reformatting it only makes the next generate a diff.
db/migrations
# Captured from the live source and committed byte-for-byte. Reformatting them would
# destroy the only thing they prove. Their .meta.json siblings are ours, and stay formatted.
db/fixtures/*.html
